** The original code structure was kept same. And the calculation of current was added in the
required function

______________Input_____________
This matlab script is used to calculate current using 
some conditions.

Followings values were expected to be input in the current calculation function:

1. The Readiation factors (alpha, epsilon) as input
2. Wind velocity as input
3. Wind direction (beta) as input
4. Average temperature
5. Absolute temperature